// Identity tables are generated from lib/auth/config.ts with Better Auth's CLI.
// Mosaic-owned domain tables will be exported here as they are introduced.
export * from "./auth-schema";
export * from "./domain-schema";
