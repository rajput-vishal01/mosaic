import { Body, Button, Container, Head, Heading, Html, Preview, Section, Text } from "react-email";

export function AgencyInvitationEmail({ agencyName, inviterName, invitationUrl }: { agencyName: string; inviterName: string; invitationUrl: string }) {
  return <Html><Head /><Preview>You have been invited to {agencyName} in Mosaic</Preview><Body style={{ backgroundColor: "#f8fafc", fontFamily: "Arial, sans-serif", padding: "32px 12px" }}><Container style={{ backgroundColor: "#ffffff", border: "1px solid #e2e8f0", borderRadius: "12px", margin: "0 auto", maxWidth: "520px", padding: "28px" }}><Heading style={{ color: "#0f172a", fontSize: "22px", margin: "0 0 16px" }}>Join {agencyName}</Heading><Text style={{ color: "#475569", fontSize: "15px", lineHeight: "24px" }}>{inviterName} invited you to access client analytics in Mosaic. Create your password using the secure link below.</Text><Section style={{ margin: "24px 0" }}><Button href={invitationUrl} style={{ backgroundColor: "#0f172a", borderRadius: "8px", color: "#ffffff", fontSize: "14px", fontWeight: "bold", padding: "12px 18px" }}>Accept invitation</Button></Section><Text style={{ color: "#64748b", fontSize: "13px", lineHeight: "20px" }}>This invitation expires in 48 hours. If you were not expecting it, no action is required.</Text></Container></Body></Html>;
}
