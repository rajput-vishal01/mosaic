import Link from "next/link";

import { listAuditEvents } from "@/features/account-grants/queries";

export default async function AuditPage() {
  const events = await listAuditEvents();
  return <main className="min-h-screen bg-slate-50 px-5 py-8 lg:px-8"><div className="mx-auto max-w-7xl"><Link className="text-sm font-medium text-slate-500 hover:text-slate-950" href="/dashboard">← Overview</Link><div className="mb-6 mt-5"><p className="text-sm font-medium text-slate-500">Platform administration</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">Audit log</h1><p className="mt-2 text-sm text-slate-500">The latest security-sensitive decisions across agencies and account access.</p></div><section className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">{events.length === 0 ? <p className="px-5 py-10 text-center text-sm text-slate-500">No audit events yet.</p> : <div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3 font-medium">Time</th><th className="px-4 py-3 font-medium">Actor</th><th className="px-4 py-3 font-medium">Agency</th><th className="px-4 py-3 font-medium">Action</th><th className="px-4 py-3 font-medium">Resource</th><th className="px-4 py-3 font-medium">Result</th><th className="px-4 py-3 font-medium">Correlation</th></tr></thead><tbody className="divide-y divide-slate-100">{events.map((event) => <tr key={event.id}><td className="whitespace-nowrap px-4 py-3 text-slate-500">{event.createdAt.toLocaleString()}</td><td className="px-4 py-3 text-slate-700">{event.actorName ?? "System"}</td><td className="px-4 py-3 text-slate-700">{event.agencyName ?? "Platform"}</td><td className="px-4 py-3 font-medium text-slate-900">{event.action}</td><td className="px-4 py-3 text-slate-500">{event.resourceType}</td><td className="px-4 py-3"><span className={`rounded-full px-2 py-0.5 text-xs font-medium ${event.result === "allowed" ? "bg-emerald-50 text-emerald-700" : "bg-red-50 text-red-700"}`}>{event.result}</span></td><td className="px-4 py-3 font-mono text-xs text-slate-500">{event.correlationId.slice(0, 8)}</td></tr>)}</tbody></table></div>}</section></div></main>;
}
